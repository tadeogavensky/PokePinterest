import { motion } from "framer-motion";
const Loader = () => {

  return (
    <div className="min-h-screen flex justify-center items-center">
      <h1 className="font-pokemon text-3xl sm:text-6xl font-bold animate-pulse">
        PokePinterest
      </h1>
    </div>
  );
};

export default Loader;
